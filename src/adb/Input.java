package adb;

import java.io.FileWriter;
import java.io.IOException;

import imgProcessing.*;

public class Input
{
	public static void GetScreen() throws InterruptedException, IOException
	{
		System.out.println("get Screen");
		/*FileWriter writer = new FileWriter("chest slots.txt");
		writer.write("slot 1 " + String.valueOf(ScanFor.chestSlot(1)) + "\n slot 2 "
				+ String.valueOf(ScanFor.chestSlot(2)) + "\n slot 3 " + String.valueOf(ScanFor.chestSlot(3))
				+ "\n,slot 4 " + String.valueOf(ScanFor.chestSlot(4)));
		writer.close();*/
	}

}
